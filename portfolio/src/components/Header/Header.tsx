import '@/components/Header/Header.sass';

const Header: React.FC = () => {
  return (
    <header data-cursor-color="black" className="header">
      <h1 className="header-title">BEN</h1>
    </header>
  );
};

export default Header;