import React from 'react';
import '@/components/ImageScroller/ImageScroller.sass';

const ImageScroller: React.FC = () => {
  return (
    <div className="container-scroller"></div>
  );
};

export default ImageScroller;
